import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    
      card:{
       padding:10,
       margin:5,
       borderRadius:20,
       backgroundColor:'#fff',
       maxWidth:'70%'
      },

    message:{
        fontSize:16,
        padding:5,
        fontWeight:'500',
        paddingBottom:10
    },

    date:{
        fontWeight:'bold',
        fontSize:14
    }
})

export default styles